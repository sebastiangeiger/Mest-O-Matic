.user_panel
	- if signed_in?
		.text
		=current_user.name
		= "is #{current_user.type}" unless current_user.unassigned?
		#logout
			%a{:href => "/logout"}
				logout
		#clear
	- elsif(request.path_info=='/login')
		Not logged in
	- else
		%a{:href => "/login"}
			login
